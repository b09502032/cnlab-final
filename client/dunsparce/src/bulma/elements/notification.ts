export function notification(element: HTMLDivElement) {
  element.classList.add("notification");
  return element;
}

export function deleteButton(element: HTMLButtonElement) {
  element.classList.add("delete");
  return element;
}
